language: go
env:
  - GO111MODULE=on
  - GOFLAGS='-mod vendor'
go:
  - 1.12.x

install: true

notifications:
  email: false

before_script:
  - go install github.com/golangci/golangci-lint/cmd/golangci-lint

script:
  - golangci-lint run
  - go test -v -race ./...